<%= form_with model: @document, local: true do |f| %>
  <div class="form-group">
    <%= f.label :content %>
    <small class="text-red d-block">
      ※全角のひらがな、全角か半角のカタカナ、数字のみ利用可能です。
      (半角カタカナは逆さ文字、全角カタカナはずれたひらがな、全角数字はずれた数字として表示されます。)
    </small>
    <%= f.text_area :content, class: 'form-control', rows: 5 %>
  </div>
  <div class="form-group">
    <%= f.label :name %>(任意)
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit 'そうしん', class: 'btn btn-pink' %>
  </div>
<% end %>
